name: Update ExoMars TGO
on:
  schedule:
    - cron: '0 9 * * 1'  # weekly Mon at 09:00 UTC
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    environment: HF
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install pandas pyarrow requests huggingface_hub[hf_xet] hf-dataset-utils
      - id: update
        run: python scripts/update-exomars-tgo.py
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
      - name: Update and push status
        run: bash scripts/push-status.sh exomars-tgo --rows ${{ steps.update.outputs.rows }}
